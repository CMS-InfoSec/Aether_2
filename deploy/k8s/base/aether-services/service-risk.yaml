apiVersion: v1
kind: Service
metadata:
  name: risk-service
  labels:
    app: risk-service
    app.kubernetes.io/name: risk-service
spec:
  selector:
    app: risk-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
