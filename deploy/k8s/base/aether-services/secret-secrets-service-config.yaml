
# The Secrets Service now sources its `SECRET_ENCRYPTION_KEY` from the
# ExternalSecret defined in `secrets/secrets-service-config-external-secret.yaml`.
# This placeholder is retained so that legacy overlays referencing the file
# continue to sync without errors.


