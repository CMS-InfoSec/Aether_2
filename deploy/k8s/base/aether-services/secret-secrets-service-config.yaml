
# This file intentionally does not define a Kubernetes Secret.
# The secrets-service encryption key is rotated manually following the
# runbook at docs/runbooks/secrets-service-key-rotation.md. GitOps overlays
# should not reference this file so that the applied key remains managed
# outside of the repository.


