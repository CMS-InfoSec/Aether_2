apiVersion: v1
kind: Secret
metadata:
  name: secrets-service-config
  labels:
    app.kubernetes.io/name: secrets-service
    app.kubernetes.io/component: api
    app: secrets-service
type: Opaque
stringData:
  # Placeholder decodes to 32 zero bytes and keeps the pod healthy until a real key is provided.
  SECRET_ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
