apiVersion: v1
kind: Service
metadata:
  name: secrets-service
  labels:
    app: secrets-service
    app.kubernetes.io/name: secrets-service
spec:
  selector:
    app: secrets-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
