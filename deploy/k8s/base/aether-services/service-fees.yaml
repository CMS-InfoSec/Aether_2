apiVersion: v1
kind: Service
metadata:
  name: fees-service
  labels:
    app: fees-service
    app.kubernetes.io/name: fees-service
spec:
  selector:
    app: fees-service
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
